$(document).ready(function () {
    /*Under the inputs in the HTML create a table element with 4 columns for First Name, Last Name, Password and E-mail and one button. When the button is click check if all inputs have values and if that is true create table with the values of the input fields else show message that all fields are required.
    You should have a new table row for every new user you make*/


    let firstName = $("#firstName");
    let lastName = $("#lastName");
    let password = $("#password");
    let email = $("#email");
    let btn = $("#btn");

})